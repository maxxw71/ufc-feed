# UFC Feed

Automated public JSON feed of upcoming UFC events and announced bouts.

The feed is generated every six hours by GitHub Actions from ESPN's public UFC schedule API and written to `upcoming.json`.

This repository contains schedule/card data only. It does not contain betting-model code, API keys, or private credentials.
