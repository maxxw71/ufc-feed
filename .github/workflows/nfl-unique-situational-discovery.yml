name: NFL unique situational discovery
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_unique_situational_discovery.py'
      - 'scripts/nfl_unique_situational_discovery_trigger.py'
permissions:
  contents: write
jobs:
  research:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run discovery
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_unique_situational_discovery.py"
      - name: Publish
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/unique_situational_discovery
          DEST="$GITHUB_WORKSPACE/nfl/unique_situational_discovery"
          mkdir -p "$DEST"
          cp "$SRC"/* "$DEST"/
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/unique_situational_discovery
          if ! git diff --cached --quiet; then
            git commit -m 'Update unique situational NFL discovery results'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
