name: BoxingScene CompuBox Archive Probe
on:
  push:
    branches: [main]
    paths:
      - 'boxing/research/probe_boxingscene_author.py'
      - '.github/workflows/boxing-boxingscene-author-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check beautifulsoup4 lxml
      - run: python boxing/research/probe_boxingscene_author.py
