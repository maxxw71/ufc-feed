name: NFL coaching deployment audit
on:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_coaching_deployment_audit.py'
      - '.github/workflows/nfl-coaching-deployment-audit.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  audit:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run coaching deployment audit
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_coaching_deployment_audit.py"
      - name: Publish results
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/coaching_deployment_audit
          DEST="$GITHUB_WORKSPACE/nfl/coaching_deployment_audit"
          mkdir -p "$DEST"
          cp "$SRC/summary.json" "$DEST/summary.json"
          cp "$SRC/report.txt" "$DEST/report.txt"
          cp "$SRC/coaching_method_deployment_audit.csv" "$DEST/coaching_method_deployment_audit.csv"
          cp "$SRC/coaching_methods_ready.csv" "$DEST/coaching_methods_ready.csv"
          cp "$SRC/coaching_methods_watch.csv" "$DEST/coaching_methods_watch.csv"
          cp "$SRC/coaching_vetoes_deployment_grade.csv" "$DEST/coaching_vetoes_deployment_grade.csv"
          cp "$SRC/coaching_primary_vetoes_ready.csv" "$DEST/coaching_primary_vetoes_ready.csv"
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/coaching_deployment_audit
          if ! git diff --cached --quiet; then
            git commit -m 'Update NFL coaching deployment audit'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
