name: MLS Arsenal Portfolio

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/mls_arsenal_portfolio.py'
      - '.github/workflows/mls-arsenal-portfolio.yml'

concurrency:
  group: mls-arsenal-portfolio
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  portfolio:
    if: github.repository == 'maxxw71/ufc-feed'
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}

      - name: Deploy and evaluate refined MLS arsenal
        shell: bash
        run: |
          set -Eeuo pipefail
          KEY=/home/appwiza-runner/.ssh/appwiza_vps
          HOST=anestishkurti92@127.0.0.1
          SSH="ssh -i $KEY -o BatchMode=yes -o ConnectTimeout=5 -o StrictHostKeyChecking=accept-new $HOST"
          D=/home/anestishkurti92/mls-predictor-v1
          cat scripts/mls_arsenal_research.py | $SSH 'set -e; D=/home/anestishkurti92/mls-predictor-v1; cat > "$D/mls_arsenal_research.py.new"; mv -f "$D/mls_arsenal_research.py.new" "$D/mls_arsenal_research.py"'
          cat scripts/mls_arsenal_portfolio.py | $SSH 'set -e; D=/home/anestishkurti92/mls-predictor-v1; cat > "$D/mls_arsenal_portfolio.py.new"; mv -f "$D/mls_arsenal_portfolio.py.new" "$D/mls_arsenal_portfolio.py"'
          $SSH 'set -Eeuo pipefail; cd /home/anestishkurti92/mls-predictor-v1; venv/bin/python -m py_compile mls_arsenal_research.py mls_arsenal_portfolio.py; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 nice -n 12 timeout 10m venv/bin/python mls_arsenal_portfolio.py'

      - name: Pull portfolio audit
        shell: bash
        run: |
          set -Eeuo pipefail
          KEY=/home/appwiza-runner/.ssh/appwiza_vps
          HOST=anestishkurti92@127.0.0.1
          mkdir -p mls/research
          ssh -i "$KEY" -o BatchMode=yes -o ConnectTimeout=5 -o StrictHostKeyChecking=accept-new "$HOST" 'cat /home/anestishkurti92/mls-predictor-v1/research/arsenal_portfolio/latest.json' > mls/research/arsenal_portfolio_latest.json

      - name: Persist portfolio audit
        shell: bash
        run: |
          set -Eeuo pipefail
          cp mls/research/arsenal_portfolio_latest.json "$RUNNER_TEMP/portfolio.json"
          git fetch origin main
          git reset --hard origin/main
          mkdir -p mls/research
          cp "$RUNNER_TEMP/portfolio.json" mls/research/arsenal_portfolio_latest.json
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add mls/research/arsenal_portfolio_latest.json
          if ! git diff --cached --quiet; then
            git commit -m 'Publish MLS arsenal portfolio audit'
            git push origin HEAD:main
          fi
