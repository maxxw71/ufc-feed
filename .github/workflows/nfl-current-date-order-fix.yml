name: NFL current date order fix (deprecated)
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical page owns this behavior
        run: |
          echo "DEPRECATED: this workflow no longer writes /sports/nfl/ directly."
          echo "NFL page ordering and structure are generated only by nfl/nfl_public_page.py through the canonical deploy/scanner hook."
