name: NFL streak bounceback division discovery
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_streak_bounceback_division_discovery.py'
      - 'scripts/nfl_streak_bounceback_division_trigger.py'
permissions:
  contents: write
jobs:
  research:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run discovery
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_streak_bounceback_division_discovery.py"
      - name: Publish
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/streak_bounceback_division_discovery
          DEST="$GITHUB_WORKSPACE/nfl/streak_bounceback_division_discovery"
          mkdir -p "$DEST"
          cp "$SRC"/* "$DEST"/
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/streak_bounceback_division_discovery
          if ! git diff --cached --quiet; then
            git commit -m 'Update streak bounceback division discovery results'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
