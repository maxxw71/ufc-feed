name: NFL calendar schedule runheavy discovery
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_calendar_schedule_runheavy_discovery.py'
permissions:
  contents: write
jobs:
  research:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run discovery
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_calendar_schedule_runheavy_discovery.py"
      - name: Publish
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/calendar_schedule_runheavy_discovery
          DEST="$GITHUB_WORKSPACE/nfl/calendar_schedule_runheavy_discovery"
          mkdir -p "$DEST"
          cp "$SRC"/* "$DEST"/
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/calendar_schedule_runheavy_discovery
          if ! git diff --cached --quiet; then
            git commit -m 'Update calendar schedule run-heavy discovery results'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
