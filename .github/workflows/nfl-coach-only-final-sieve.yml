name: NFL coach-only final sieve
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_coach_only_final_sieve.py'
permissions:
  contents: write
jobs:
  audit:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run final sieve
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_coach_only_final_sieve.py"
      - name: Publish
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/coach_only_final_sieve
          DEST="$GITHUB_WORKSPACE/nfl/coach_only_final_sieve"
          mkdir -p "$DEST"
          cp "$SRC"/* "$DEST"/
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/coach_only_final_sieve
          if ! git diff --cached --quiet; then
            git commit -m 'Update coach-only final sieve results'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
