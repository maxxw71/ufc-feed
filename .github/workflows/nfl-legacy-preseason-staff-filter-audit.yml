name: NFL legacy preseason staff filter audit
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/nfl_legacy_preseason_staff_filter_audit.py'
permissions:
  contents: write
jobs:
  audit:
    runs-on: [self-hosted, Linux, X64, appwiza]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Run audit
        shell: bash
        run: |
          set -Eeuo pipefail
          /home/anestishkurti92/nfl-predictor-v1/venv/bin/python "$GITHUB_WORKSPACE/scripts/nfl_legacy_preseason_staff_filter_audit.py"
      - name: Publish
        shell: bash
        run: |
          set -Eeuo pipefail
          SRC=/home/appwiza-runner/nfl-context-data/legacy_preseason_staff_filter_audit
          DEST="$GITHUB_WORKSPACE/nfl/legacy_preseason_staff_filter_audit"
          mkdir -p "$DEST"
          cp "$SRC"/* "$DEST"/
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add nfl/legacy_preseason_staff_filter_audit
          if ! git diff --cached --quiet; then
            git commit -m 'Update NFL legacy preseason staff filter audit'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
