name: Appwiza UFC U7 deploy (deprecated)
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical UFC pipeline owns live page
        run: |
          echo "DEPRECATED: this workflow no longer writes the UFC watcher or public page."
          echo "Use the canonical UFC deployment and live watcher; U7 and the method key are already incorporated there."
